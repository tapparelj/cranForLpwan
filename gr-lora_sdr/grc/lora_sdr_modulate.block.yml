# auto-generated by grc.converter

id: lora_sdr_modulate
label: Modulate
category: '[LoRa_TX]'

parameters:
-   id: sf
    label: SF
    dtype: int
    default: 'sf'
-   id: samp_rate
    label: Samp_rate
    dtype: int
-   id: bw
    label: BW
    dtype: int
-   id: sync_words
    label: Sync words
    dtype: int_vector
    hide: part
    default: 0x12
-   id: frame_zero_padd
    label: Frame zero padding
    hide: part
    dtype: int
    default: int(20*2**sf*samp_rate/bw)
-   id: preamb_len
    label: Preamble length
    hide: part
    dtype: int
    default: 8
-   id: n_frames
    label: Number of Frames
    hide: part
    dtype: int
    default: 0

inputs:
-   domain: stream
    dtype: int

outputs:
-   domain: stream
    dtype: complex

templates:
    imports: import gnuradio.lora_sdr as lora_sdr
    make: lora_sdr.modulate(${sf}, ${samp_rate}, ${bw}, ${sync_words}, ${frame_zero_padd},${preamb_len},${n_frames})
    callbacks:
        - set_sf(${sf})
asserts:
- ${ (samp_rate/bw).is_integer()}
- ${frame_zero_padd>=0}

documentation: |-
    Add the preamble to the frame and modulate lora symbols according to the input decimal values.
        Parameters:
            sf: spreading factor
            samp_rate: sampling rate (need to be a integer multiple of the bandwidth)
            Bw: LoRa bandwidth
            Frame zero padding: zero padding append after each frame. Value given in number of samples with a minimum of 1
            Preamble length: number of preamble upchirps. A values in [5,...,65535] with 8 being the default for LoRaWAN
            Number of Frames: number of frames to send before exiting. 0 corresponds to never exit.
        Input:
            in: stream of decimal symbol values
        Output:
            out: stream of complex valued samples

file_format: 1
